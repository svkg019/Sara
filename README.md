# Sara
Sara is a command-line chatbot that uses OpenAI’s chat completion API and the GPT-4 model to generate natural and engaging conversations. You can chat with Sara about various topics, such as hobbies, movies, music, sports, etc. She can also answer your questions, and tell you jokes, stories, facts, and more.

## Installation
To install Sara, follow these steps:
- Clone the repository from GitHub using the following command:
~~~
git clone https://github.com/svkg019/Sara.git
~~~

- Then, open the `.env` file and update the API key.
- After successfully set the API key open the terminal in the current directory and run the following command:
~~~
python chatBotSara.py
~~~
